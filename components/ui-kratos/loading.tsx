export default function Loading(){
    return(
        <div className="h-screen w-screen flex items-center justify-center">
            <h1 className={`text-6xl font-bold tracking-tighter bg-gradient-to-r from-blue-400 to-blue-700 text-transparent bg-clip-text`}>CARREGANDO...</h1>        
        </div>
    )
}