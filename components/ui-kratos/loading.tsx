export default function Loading(){
    return(
        <div className="h-screen w-screen flex items-center justify-center">
            <h1 className={`text-6xl font-bold tracking-tighter bg-gradient-to-r from-red-700 to-red-900 text-transparent bg-clip-text`}>CARREGANDO...</h1>        
        </div>
    )
}